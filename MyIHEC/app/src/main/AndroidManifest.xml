<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ihecso.pfe.myihec">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".SendmsgActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".EventActivity" />
        <activity android:name=".ResultatActivity" />
        <activity android:name=".DisciplineActivity" />
        <activity android:name=".EmploiEtudiantActivity" />
        <activity android:name=".GroupActivity" />
        <activity android:name=".TeacherEmploiActivity" />
        <activity android:name=".NewsItemActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>